<%= form_for(item_photo) do |f| %>
  <% if item_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_photo.errors.count, "error") %> prohibited this item_photo from being saved:</h2>

      <ul>
      <% item_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %>
    <%= f.text_field :item_id %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.text_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
